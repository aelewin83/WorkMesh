# Recovery Model

Recovery must not weaken encryption by sending raw device keys to Relai.

Phase 2 provides a recovery package placeholder and UX copy. The intended direction is an encrypted recovery package that users store privately, with future support for recovery contacts or passkey-backed local key unlock.

If a user loses all device keys and recovery packages, Relai may not be able to decrypt old content.
